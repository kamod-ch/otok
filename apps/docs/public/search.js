const input=document.getElementById('search');const index=JSON.parse(document.getElementById('search-index')?.textContent||'[]');let box;function close(){box?.remove();box=undefined}input?.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();close();if(!q)return;const matches=index.filter(p=>(p.title+' '+p.description).toLowerCase().includes(q)).slice(0,8);box=document.createElement('div');box.id='search-results';box.setAttribute('role','listbox');box.innerHTML=matches.length?matches.map(p=>`<a href="${p.url}"><strong>${p.title}</strong><span>${p.description}</span></a>`).join(''):'<span>No results</span>';document.body.append(box)});document.addEventListener('keydown',e=>{if(e.key==='Escape')close()});
