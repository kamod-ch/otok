---
"@kamod-ch/otok": patch
---

Stabilize island hydration during soft navigation: unmount replaced hydrated roots, decouple navigation completion from deferred strategies, invalidate stale async imports, and run DOM swaps inside view transitions (respecting reduced motion).
