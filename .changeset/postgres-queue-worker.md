---
"@kamod-ch/otok-queue": minor
"@kamod-ch/otok": patch
---

Add PostgreSQL queue provider with lease-based claiming, worker runtime, cron dedupe, and `PostgresIdempotencyStore` for durable HTTP idempotency. Document at-least-once delivery in ADR-001.
