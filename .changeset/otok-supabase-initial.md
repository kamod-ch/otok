---
"@kamod-ch/otok-supabase": minor
"@kamod-ch/otok-registry": patch
---

Add `@kamod-ch/otok-supabase` with SSR cookie auth, Hono middleware, plugin factory, loader helpers, and auth routes. Register the extension in the Otok registry and add the `with-supabase` example.
