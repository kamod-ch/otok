---
"otok": minor
---

Add `resolveOtokManifest()` for Edge/Workers, keep `readOtokManifest()` Node-only, harden `createOtokApp` against static `node:module` imports, and ship a standalone Cloudflare Workers example with `pnpm smoke:cloudflare`.
