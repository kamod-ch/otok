---
"@otok/vite-plugin": minor
"create-otok": patch
---

Add a typed `route()` URL builder to `virtual:otok-routes`, generated route file patterns, and runtime URL encoding for dynamic, catch-all, optional, grouped, query, and hash routes.
