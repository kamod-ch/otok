---
"otok": minor
---

Add shared response helpers for loaders and future actions, including `json()`, validation-style `fail(status, failure)`, native `Response` passthrough, and `isOtokResponse()`.
