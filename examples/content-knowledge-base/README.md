# Knowledge base example

Articles with categories, tags, and cross-references.

```ts
import { knowledgeBasePreset } from "@kamod-ch/otok-content/presets";

export const collections = knowledgeBasePreset;
```

References between articles are validated at build time.
