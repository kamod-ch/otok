FROM node:22-alpine
WORKDIR /app
COPY . .
RUN corepack enable && pnpm install --frozen-lockfile && pnpm --filter @kamod-ch/otok-queue build
CMD ["node", "-e", "console.log('Configure worker entrypoint with runQueueWorker + createPostgresQueueProvider')"]
