# Docs example

Documentation site pattern with ordered sidebar pages.

```ts
import { docsPreset } from "@kamod-ch/otok-content/presets";

export const collections = docsPreset;
```

Load docs sorted by `order` field for navigation sidebars.
