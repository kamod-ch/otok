# Database connection (server-only — never expose to client)
DATABASE_URL=sqlite://./data/saas.db

# Auth secret (min 32 characters in production)
AUTH_SECRET=dev-secret-at-least-32-characters-long!!

# Public site URL for SEO plugin
APP_URL=http://localhost:5173
