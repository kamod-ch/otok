# otok

Hono + Preact Islands framework runtime.

See the repository documentation in `apps/docs` and `docs/` for routing, server rendering, actions, middleware, islands, testing, and deployment guides.
